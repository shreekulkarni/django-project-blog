This is a webserver that performs the basic functions on admin control over blog posts as well as the API functions for the posts. 
Functions like viewing posts, viewing details of posts and deleting posts.
A small django db is also used
